* {
    /* `otf-font-awesome` is required to be installed for icons */
    font-family: FontAwesome, Roboto, Helvetica, Arial, sans-serif;
    font-size: 13px;
}

window#waybar {
    background-color: #{{color.client.default.background}};
    border-bottom: 3px solid #{{color.by-preference.n00}};
    color: #{{color.button.default.foreground}};
    transition-property: background-color;
    transition-duration: .5s;
}

window#waybar.hidden {
    opacity: 0.2;
}

button {
    /* Use box-shadow instead of border so the text isn't offset */
    box-shadow: inset 0 -3px transparent;
    /* Avoid rounded borders under each button name */
    border: none;
    border-radius: 0;
}

#workspaces button {
    padding: 0 5px;
    color: #{{color.button.default.foreground}};
    background-color: transparent;
}

#workspaces button:hover {
    background-color: #{{color.button.default.background}};
}

#workspaces button.focused {
    background-color: #{{color.button.focused.background}};
    box-shadow: inset 0 -3px #ffffff;
}

#workspaces button.urgent {
    background-color: #{{color.button.urgent.background}};
}

#mode {
    background-color: #{{color.button.default.background}};
    box-shadow: inset 0 -3px #ffffff;
}

.module {
    padding: 0 10px;
    color: #{{color.button.default.foreground}};
    background-color: transparent;
}

#window,
#workspaces {
    margin: 0 4px;
}

/* If workspaces is the leftmost module, omit left margin */
.modules-left > widget:first-child > #workspaces {
    margin-left: 0;
}

/* If workspaces is the rightmost module, omit right margin */
.modules-right > widget:last-child > #workspaces {
    margin-right: 0;
}

@keyframes blink {
    to {
        background-color: #ffffff;
        color: #000000;
    }
}


label:focus {
    background-color: #{{color.button.focused.background}};
}


/****
 ****
 **** Module styles, in alphabetical order
 ****
 ****/



/*
 * battery
 */

#battery {
    color: #{{color.button.default.foreground}};
    background-color: #{{color.button.default.background}};
}

#battery.charging, #battery.plugged {
    color: #{{color.button.default.foreground}};
    background-color: #{{color.button.default.background}};
}

/* Using steps() instead of linear as a timing function to limit cpu usage */
#battery.critical:not(.charging) {
    color: #{{color.button.default.foreground}};
    background-color: #{{color.button.warning.background}};
    animation-name: blink;
    animation-duration: 0.5s;
    animation-timing-function: steps(12);
    animation-iteration-count: infinite;
    animation-direction: alternate;
}

/*
 * clock
 */

#clock {
    color: #{{color.by-preference.n00}};
}

/*
 * cpu
 */

#cpu {
    color: #{{color.by-preference.n04}};
}


/*
 * disk
 */

#disk {
    color: #{{color.by-preference.n03}};
}

/*
 * keyboard-state
 */

#keyboard-state {
    padding: 0 0px;
    margin: 0 5px;
    min-width: 16px;
    color: #{{color.by-preference.n03}};
}

#keyboard-state > label {
    padding: 0 5px;
}

#keyboard-state > label.locked {
    background-color: rgba(0, 0, 0, 0.2);
}

/*
 * memory
 */

#memory {
    color: #{{color.by-preference.n03}};
}

/*
 * network
 */

#network {
    color: #{{color.by-preference.n01}};
}

#network.disconnected {
    color: #{{color.button.warning.foreground}};
    background-color: #{{color.button.warning.background}};
}

/*
 * niri/language
 */

#language {
    padding: 0 5px;
    margin: 0 5px;
    min-width: 16px;
    color: #{{color.by-preference.n03}};
}

/*
 * niri/window
 */
#window {
    color: #{{color.by-preference.n01}};
}

/*
 * niri/workspaces
 */

/*
 * pulseaudio
 */

#pulseaudio {
    color: #{{color.by-preference.n05}};
}

#pulseaudio.muted {
    color: #{{color.button.inactive.foreground}};
    background-color: #{{color.button.inactive.background}};
}

/*
 * temperature
 */

#temperature {
    color: #{{color.by-preference.n02}};
}

#temperature.critical {
    color: #{{color.button.urgent.foreground}};
    background-color: #{{color.button.urgent.background}};
}
